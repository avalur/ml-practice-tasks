import numpy as np


def null_with_one(n: int, i: int) -> np.ndarray:
    """
    Return a length-``n`` vector of zeros (dtype float) whose element at index
    ``i`` is set to ``1``.

    Example: ``null_with_one(10, 4)`` is a size-10 null vector whose fifth
    value (index 4) is 1.

    :param n: length of the vector
    :param i: index to set to 1 (``0 <= i < n``)
    :return: the resulting array
    """
    raise NotImplementedError("Your code here")
