import numpy as np


def normalize_rows(X: np.ndarray) -> np.ndarray:
    """
    Scale each row of ``X`` to unit L2 norm. ``X`` is ``(m, n)``; return an
    array of the same shape. Assume no zero rows. No loops.

    :param X: 2D array of shape (m, n)
    :return: row-normalized array of shape (m, n)
    """
    raise NotImplementedError("Your code here")
