import numpy as np


def replace_max(x: np.ndarray) -> np.ndarray:
    """
    Return a copy of 1D ``x`` with its maximum element set to ``0`` (first
    occurrence if tied). ``x`` itself must not be modified. No loops.

    :param x: 1D array
    :return: copy of ``x`` with the max replaced by 0
    """
    raise NotImplementedError("Your code here")
