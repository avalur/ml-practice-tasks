/** @type {import('next').NextConfig} */
const nextConfig = {
  // Pyodide assets and COOP/COEP headers are added in a later phase when the
  // in-browser runner lands. Kept minimal for now.
};

export default nextConfig;
