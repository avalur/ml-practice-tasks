import numpy as np


def flatten(X: np.ndarray) -> np.ndarray:
    """
    Return a 1D array containing all elements of ``X`` in row-major (C)
    order — the first row, then the second, and so on.

    :param X: an array of any shape
    :return: 1D array of all elements of ``X`` in C order
    """
    raise NotImplementedError("Your code here")
